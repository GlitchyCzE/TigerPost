const CONFIG = {
    localPort: 3000,

    database: {
        host: "localhost",
        database: "tigerpost",
        username: "tigerpost",
        password: "changeme"
    }
}

module.exports = CONFIG;
